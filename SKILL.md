---
name: ask-poteto
description: 診斷某一次（或同一件工作重開的多次）Claude Code 對話哪裡卡住。讀完整 session 紀錄（含使用者對話與工具呼叫），用 poteto「糾正代理時先想哪一層」的框架，找出問題出在程式碼、自動檢查／hooks、CLAUDE.md 等規則、技能，還是使用者提示本身；也主動找沒人糾正、但可能出錯的「沉默的失敗」；檢查代理知不知道使用者真正的目標、優先順序由誰決定、工作有沒有對準方向（目標對齊）；再依 poteto 三支柱（驗證、品質技能、對代理友善的結構）體檢這個專案缺什麼、下一步該建什麼。每條建議都標明影響範圍（全域或單一專案）。使用者說「診斷這個 session」「看看那次對話出了什麼問題」「為什麼 Claude 一直做錯」「檢查我的 CLAUDE.md 有沒有問題」「分析我的 prompt」時使用。
---

# Session 診斷

把一次對話當成「糾正清單」：每次使用者糾正或介入、或 Claude 出錯，都問這個錯本來可以在哪一層被擋下。但沒人糾正不代表沒出錯，所以也要主動找「沉默的失敗」：Claude 宣稱完成卻沒有證據、會吞掉錯誤的指令寫法、步驟被悄悄省略。接著看 poteto 的三根支柱——代理能不能自己驗證、有沒有技能教它照使用者期待的方式做、專案結構是否讓它預設就做對——說明下一步該建什麼。最後看方向：專案的目標是不是使用者自己確認的、代理做的事有沒有對準它——錯誤修得再乾淨，方向錯了一樣是在原地打轉。重活交給一個 subagent 做，讓它有乾淨的閱讀空間讀完整份紀錄。

框架取自 poteto（Lauren Tan）的演講（https://x.com/poteto/status/2102050467505430555），這裡是非官方的整理與改寫，與本人無關。

下面的 `<SKILL_DIR>` 指這份 SKILL.md 所在的資料夾（Claude Code 載入技能時會顯示它的 base directory；使用者層安裝通常是 `~/.claude/skills/ask-poteto`）。

## 兩條硬規則

1. **診斷哪個對話，只由使用者決定。** 不要自己挑「看起來問題最多」的對話，也不要為了示範或測試去讀使用者沒指定的對話。對話紀錄是使用者的私人資料。需要測試時，先問使用者，或只用 `current`（目前這個對話）。
2. **每條修正建議都要標出影響範圍，預設放在專案層。** 使用者的專案用途可能各不相同（寫程式、資料庫、資料處理、寫文件……），全域設定會影響所有專案。範圍判斷見 `references/rubric.md` 第六節。

## 步驟

### 1. 確定要診斷哪些 session

參數可以是一個或多個 session ID（前幾碼即可）、`.jsonl` 路徑，或 `current`。

- **沒給參數**：只列清單讓使用者挑，不要開始診斷。可以用專案關鍵字篩選：

  ```bash
  python3 <SKILL_DIR>/scripts/condense_session.py list --project <關鍵字> --limit 20
  ```

  「疑似糾正」欄是關鍵字計數，只供參考。
- **同一件工作被重開過**（標題像「恢復之前的任務」「Resume interrupted session」，或開始與結束時間首尾相接）：告訴使用者有哪些相關的 ID，建議一起診斷，由使用者決定要不要一起。
- **`current`**：目前這個對話。直接把 `current` 交給腳本（見步驟 2），它會找紀錄裡工作目錄相同、最近更新的那份；不要自己拼專案資料夾名稱（Claude Code 會把路徑裡的底線、點、空白等都換成 `-`，拼不準）。工作目錄在對話中換過時，加 `--cwd <對話開始時的資料夾>`。先說明本輪還在進行中，不會被診斷到。
- **最後更新時間是剛剛的 session**：可能還開在別的視窗裡。提醒使用者，診斷只會看到目前為止的內容。

### 2. 產生摘要

放在暫存位置（有 scratchpad 就用 scratchpad，否則 `mktemp -d`），不要放進使用者的專案。多個 ID 會依開始時間合併成一份，標成 S1、S2……：

```bash
python3 <SKILL_DIR>/scripts/condense_session.py digest <ID1> [<ID2> ...] --out <暫存資料夾>/digest.md
```

摘要只保留文字：使用者和 Claude 的完整對話、縮短的工具呼叫與錯誤。圖片會丟掉，看起來像金鑰或密碼的字串會被遮蔽。摘要開頭若列出 subagent 紀錄，而且診斷需要，就用同一支腳本另做摘要。

### 3. 列出當時生效的指示檔，並分清全域與專案

從摘要開頭的「工作目錄」與「載入的指示檔」，整理出存在的路徑（用 `ls` 確認），分兩組：

- **全域**（影響所有專案）：`~/.claude/CLAUDE.md`、`~/.claude/rules/`、`~/.claude/settings.json` 的 hooks 與權限、`~/.claude/skills/`，以及摘要裡顯示有被載入的其他使用者層指示檔（例如家目錄的 `AGENTS.md`）
- **專案**（只影響這個專案）：工作目錄與其上層（家目錄除外）的 `CLAUDE.md`、`CLAUDE.local.md`、`AGENTS.md`（若有被載入）、`.claude/CLAUDE.md`、`.claude/settings*.json`，以及記憶資料夾 `~/.claude/projects/<專案>/memory/`

也列出**規劃與進度檔**（例如 `task.md`、`TODO.md`、`PLAN.md`、`ROADMAP.md`、`docs/` 裡的規劃文件、README），供目標對齊檢查使用。

另外，從工作目錄的內容與指示檔判斷這個專案是做什麼的（寫程式、資料處理、寫文件……），交給 subagent 作為判斷範圍的依據。

### 4. 派一個 subagent 診斷

使用者要求這個 skill，就等於授權派一個 subagent。用 `general-purpose` 類型，交代以下內容：

```
你是 Claude Code 對話的診斷員，只能讀檔、不能修改任何檔案。

1. 先完整讀診斷準則：<SKILL_DIR>/references/rubric.md
2. 完整讀對話摘要：<digest.md 路徑>（檔案大就分段讀，要讀到最後）
   需要細節時可回頭查原始紀錄：<.jsonl 路徑們>（很大，用 grep 找，不要整份讀）
3. 全域指示檔：<清單>
   專案指示檔：<清單>
   規劃與進度檔：<清單>
   這個專案的用途：<一句話>
4. 使用者特別想知道的：<使用者提到的疑問；沒有就寫「無」>

依準則第五節的格式回報。每個發現都要有摘要中的輪次、時間與原文短引；
沒有證據的推測標成「低信心」或不要列。每條修正建議都要依準則第六節標出範圍。
三支柱體檢依準則第七節，建議要具體到可以直接動手（名稱、位置、內容）。
目標對齊依準則第八節：目標寫在哪、誰寫的、和使用者親口說的是否一致、優先順序誰定的。
```

### 5. 核對後再交給使用者

- 拿 subagent 引用的原文，至少抽查最嚴重的 2–3 條（在 digest.md 裡 grep），沉默的失敗也至少抽查 1 條；目標對齊的落差要親自讀過兩邊原文；範圍建議標為「全域」的，全部都要抽查。找不到的就刪掉或標註。
- 依使用者的語言與溝通偏好重新整理：先講結論與「下次該改什麼」，再講細節。不要原封不動貼上 subagent 的報告。
- 修正建議用表格列出：修什麼、放全域還是專案、對其他類型專案的影響、能否撤回。
- 只提建議、不動手。使用者同意後才改檔；改之前先讀目標檔案。
- 最後刪除暫存的摘要檔（裡面有完整對話內容）。

## 注意

- 這是找原因，不是找人負責。使用者提示分析要公平：分清「提示真的缺資訊」和「Claude 本該自己查到或推斷出來」。
- 關鍵字標記只是提示：很多真正的糾正不會被標到，也有很多被標到的只是一般問句。
- 沒有糾正點不等於沒問題。報告要說清楚是「查過了，確實沒問題」，還是「沒人糾正，但有 N 處宣稱沒有證據」。
- 「糾正與介入點」貼近 poteto 的原意；準則裡標為「延伸」的類別和「沉默的失敗」是本技能自己加的，不是她的說法。
- 目標對齊有問題時，先講方向、再講錯誤。建議如果全是修錯誤，使用者會把時間花在支援工作上，卻離目標一樣遠。目標只能由使用者確認，可以擬草稿，不能代為決定。
- 「目標對齊」和「沉默的失敗」一樣，是本技能的延伸，不是 poteto 的說法。
- 單次對話的發現可能只是個案。若要建議全域修改，最好是在多個專案、多次對話都看到同樣模式；只看過一次，就先建議放專案層。
